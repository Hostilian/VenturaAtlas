# Agent Manifest: `autonomous-orchestration-agent`

## Mission & Scope
Task graph scheduling, bounded execution CLI control, and worktree lifecycle management.

## Owned File Paths
`data/agent-task-graph.json, scripts/va_runtime/orchestration/`

## Verification Contract
Requires clean pass on `npm run check-consistency` and `npm run test:unit`.
