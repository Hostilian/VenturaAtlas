# Agent Manifest: `data-integrity-agent`

## Mission & Scope
Schema enforcement, ID allocation, dataset completeness, metadata generation, and referential integrity.

## Owned File Paths
`data/ideas.json, data/ideas.schema.json, data/repository-meta.json`

## Verification Contract
Requires clean pass on `npm run check-consistency` and `npm run test:unit`.
