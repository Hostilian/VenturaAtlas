# Agent Manifest: `provider-router-agent`

## Mission & Scope
Multi-provider registry, capability-aware model scheduling, key pool rotation, and circuit breaker health management.

## Owned File Paths
`config/providers.json, scripts/va_orchestrator.py`

## Verification Contract
Requires clean pass on `npm run check-consistency` and `npm run test:unit`.
