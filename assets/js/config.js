/* Venture Atlas OS — Runtime Configuration (v2.3.0) */
window.VA_CONFIG = window.VA_CONFIG || {
  firebase: null // Replace with { apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId } when deploying live Firebase instance
};
