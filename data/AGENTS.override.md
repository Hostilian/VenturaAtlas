# Data-scope instructions

- Treat `ideas.json` as canonical and discover staging/lifecycle locations from executable repository contracts.
- Do not edit canonical records directly; use the authorized publisher and atomic I/O.
- Preserve IDs, lineage, contradictions, nulls, legacy cohorts, and negative outcomes.
- Never infer a score scale from observed values or convert missing/unknown into a neutral default.
- Validate schemas, relationships, score methods, and public projection after authorized changes.
- Review large JSON diffs semantically and emit migration receipts for cohort changes.
