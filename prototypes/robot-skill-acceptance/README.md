# Robot Skill Acceptance experiment harness

This harness records physical acceptance runs; it intentionally does not invent results. Run `python matrix.py` to create a blank matrix and `python matrix.py --add ...` to append an observed run.
